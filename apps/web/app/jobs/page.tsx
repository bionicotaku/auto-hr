export default function JobsPage() {
  return (
    <main className="min-h-screen bg-slate-50 px-6 py-12">
      <div className="mx-auto max-w-5xl rounded-[28px] border border-slate-200 bg-white p-10 shadow-[0_24px_64px_rgba(15,23,42,0.08)]">
        <p className="text-sm font-semibold uppercase tracking-[0.22em] text-sky-700">Jobs</p>
        <h1 className="mt-3 text-3xl font-semibold tracking-tight text-slate-950">岗位工作台</h1>
        <p className="mt-4 max-w-2xl text-sm leading-7 text-slate-600">
          Phase 2 只提供最小页面骨架。后续会在这里接入 Job 列表、创建入口和筛选工具栏。
        </p>
      </div>
    </main>
  );
}
